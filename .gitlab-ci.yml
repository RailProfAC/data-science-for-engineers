image: ntwrkguru/pandoc-gitlab-ci

stages:
  - build

build:
  stage: build
  script:
     - pandoc Intro/Seminarinhalte.md -o Seminarinhalte.html
     - pandoc -s Intro/Seminarinhalte.md -o Seminarinhalte.pdf
  artifacts:
    paths:
       - "*.html"
       - "*.pdf"
  tags:
    - shared-default